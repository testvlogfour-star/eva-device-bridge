// Top-level build file. Nothing project-wide needed beyond plugin versions,
// which are declared (with apply false) so the :app module can apply them.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
